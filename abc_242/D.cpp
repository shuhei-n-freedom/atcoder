#include<bits/stdc++.h>
using namespace std;
using ll = long long;
int main(){
    string S;
    cin >> S;
    ll Q;
    cin >> Q;
    for(ll i = 0; i < Q; i++){
        ll t;
        ll k;
        ll key = k / (pow(2, t));
        ll res = k % (pow(2, t));
        
    }
}